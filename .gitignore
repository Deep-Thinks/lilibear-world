# —— 敏感（含密钥，绝对不要进 git）——
.env
.env.*
!.env.example

# —— 运行时数据（每台机器自己生成，不要进 git）——
lilibear.db
lilibear.db-wal
lilibear.db-shm
/logs/
/tmp/

# —— 工具临时产物 ——
__pycache__/
*.pyc
.playwright-mcp/
.claude/

# —— 本地调试截图 / 二进制 dump（保留站点用的 lilibear_logo.png）——
*.png
!lilibear_logo.png
*.original.png
e2e_*.bin
e2e_*.png
ui-*.png
